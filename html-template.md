# Research Slides HTML Template

Reference architecture for HTML decks generated by the `research-slides` Skill.

This file defines the **implementation contract**. `SKILL.md` defines the workflow; this file defines what a valid generated deck should look like.

## Required Architecture

Every deck must use this hierarchy:

```html
<div class="deck-viewport">
  <main class="deck-stage" id="deckStage">
    <section class="slide active visible" data-slide="1">
      ...